query Me {
  me {
    id
    email
  }
}

query Wedding {
  wedding {
    ...WeddingInfo
  }
}

query Guests {
  guests {
    ...GuestInfo
  }
}

query Guest($id: ID!) {
  guest(id: $id) {
    ...GuestInfo
  }
}
